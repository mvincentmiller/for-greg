mutation subscriptionAddressUpdate(
  $accessToken: String!
  $addressInput: AddressUpdateInput!
) {
  subscriptionAddressUpdate(accessToken: $accessToken, address: $addressInput) {
    address {
      id
      rechargeId
      name
      address1
      address2
      city
      state
      zipcode
    }
  }
}
